from django.urls import path

from memo.memorizer.views import (
    #memorize_app_view,
)

app_name = "memorizer"

urlpatterns = [
    #path("memorize/app", view=memorize_app_view, name="memorize_app_view"),
]
